# Narrative — TTL expired (alias)

See `execution-attempt-selection-expired.narrative.md`.

`selectionExpiresAt` in the past → Start must refuse with `AGENT_SELECTION_EXPIRED`; require fresh `SelectExecutionAgent`.
